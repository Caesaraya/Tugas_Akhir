import 'package:flutter/material.dart';

class KelolaProdukMobile extends StatelessWidget {
  const KelolaProdukMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}